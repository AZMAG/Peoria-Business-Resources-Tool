/**
 * JavaScript File
 * @Desc: Main
 */

require([
        "mag/config",
        "mag/map",
    ], function(config) {
        $(document).ready(function() {
            "use strict";



        });

        return;
    }

);
