/**
 * JavaScript File
 * @Desc: Main
 */

require([
        "mag/config",
        "mag/map",
        "mag/widgits"
    ], function(config) {
        $(document).ready(function() {
            "use strict";



        });

        return;
    }

);
